pipeline {
  
  agent any 
    stages {
          stage ('INICIANDO PIPELINE'){
           
                 steps('INICIO DO STEP'){
                    
                   echo "Dentro do Step"
                 }
          }
      }
  
}
